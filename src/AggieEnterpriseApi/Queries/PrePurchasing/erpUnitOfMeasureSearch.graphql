query erpUnitOfMeasureSearch ($filter: ErpUnitOfMeasureFilterInput!) {
    erpUnitOfMeasureSearch (filter: $filter) {
        metadata {
            sort
            limit
            returnedResultCount
            nextStartIndex
            totalResultCount
        }
        data {
            uomCode
            name
        }
    }
}