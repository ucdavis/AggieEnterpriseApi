query erpUserSearch ($filter: ErpUserFilterInput!) {
    erpUserSearch (filter: $filter) {
        metadata {
            sort
            limit
            returnedResultCount
            nextStartIndex
            totalResultCount
        }
        data {
            userId
            email
            active
        }
    }
}