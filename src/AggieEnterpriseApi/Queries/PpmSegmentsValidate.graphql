query ppmSegmentsValidate($segments: PpmSegmentInput!) {
  ppmSegmentsValidate(segments: $segments) {
    validationResponse {
        errorMessages
        messageProperties
        valid
    }
  }
}